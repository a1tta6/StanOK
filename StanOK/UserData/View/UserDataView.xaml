<Window x:Class="StanOK.UserData.View.UserDataView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:StanOK.UserData.View"
        mc:Ignorable="d"
        Title="UserDataView" Height="450" Width="800">
    <Grid>
        <DataGrid ItemsSource="{Binding LoginModel}" AutoGenerateColumns="False">
            <DataGrid.Columns>
            <DataGridTextColumn 
                Header="Логин"
                Binding="{Binding  Path=Login}" Width="*" />
            <DataGridTextColumn  
                Header="Пароль" 
                Binding="{Binding  Path=Password}" 
                Width="*" />
            <DataGridTextColumn 
                Header="Роль" 
                Binding="{Binding Path=Role}" 
                Width="*" />
                </DataGrid.Columns>
        </DataGrid>
        <Button Content="Назад" HorizontalAlignment="Left" 
                Height="50" 
                Width="100" 
                Margin="93,361,0,23"/>
        <Button Content="Обновить" HorizontalAlignment="Left" 
                Height="50" 
                Width="100" 
                Margin="262,361,0,23"/>
        <Button Content="Удалить" HorizontalAlignment="Left" 
                Height="50" 
                Width="100" 
                Margin="448,361,0,23"/>
        <Button Content="Добавить" HorizontalAlignment="Left" 
                Height="50" 
                Width="100" 
                Margin="623,361,0,23"/>
    </Grid>
</Window>
